export type Point = {
    x: number;
    y: number;
};
export type Contour = Point[];
type RotatedRect = {
    center: Point;
    size: {
        width: number;
        height: number;
    };
    angle: number;
};
export declare function minAreaRect(contour: Contour): RotatedRect;
declare function findContours(src: number[][], contours: Point[][], method?: string): void;
export { findContours };
