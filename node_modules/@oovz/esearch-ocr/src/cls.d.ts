import type { OrtOption } from "./main";
import { type SessionType } from "./untils";
type ortType = OrtOption["ort"];
export { Cls };
declare function Cls<DicI>(img: ImageData, ort: ortType, session: SessionType, dic: DicI[], w: number, h: number): Promise<DicI>;
